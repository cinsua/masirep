import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { MapPin } from "lucide-react";

// Force dynamic rendering to avoid build-time static generation issues
export const dynamic = 'force-dynamic';

export default function UbicacionesPage() {
  return (
    <div>
      <div className="flex items-center space-x-2 mb-6">
        <MapPin className="h-6 w-6 text-primary" />
        <h1 className="text-3xl font-bold text-foreground">Ubicaciones</h1>
      </div>

      <Card>
        <CardHeader>
          <CardTitle>Gestión de Ubicaciones</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-muted-foreground">
            Módulo de gestión de ubicaciones y almacenamiento en construcción...
          </p>
        </CardContent>
      </Card>
    </div>
  );
}